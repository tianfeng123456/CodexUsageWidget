# 当前验证报告

更新时间：2026-08-03（Asia/Shanghai）

本报告只描述当前工作区与下列发布包，不沿用旧哈希的 UI 或性能结论。

```text
文件：dist\CodexUsageWidget.exe
版本：1.2.0.0
产品版本：1.2.0+752836452d4c8c97c08656f47842c4279b9f827a
大小：164,493,889 字节（156.87 MiB）
SHA-256：1211FA8FC6B8E316E94C537D5BFFD1922C885241B67AC36E48DCEE2FC390FFB7
发布：win-x64、自包含、单文件、未裁剪
代码签名：未签名
```

## 当前结论

当前实现已经通过自动化、真实日志隔离重建、正式 SQLite 完整性、单实例黑盒、
65 秒收起静默测试，以及解锁桌面上的 UI、待机样式、主题和双语黑盒验收。
所有机器可读证据均记录同一份 v1.2.0 EXE 的哈希。

## 自动化与构建

| 项目 | 结果 |
|---|---|
| 完整 Release 测试 | 345/345 通过，0 失败、0 错误、0 中止 |
| 重复稳定性测试 | 完整测试套件连续运行 20 轮，共 6,900 次测试执行，全部通过 |
| 严格构建 | `-warnaserror` 通过，0 警告、0 错误 |
| 全规则分析 | 988 条原始提示去重核查；无编译错误，未发现新的可利用安全问题或未闭合资源所有权 |
| 覆盖率 | 行覆盖 93.09%（4,289/4,607），分支覆盖 90.32%（1,493/1,653） |
| 离线依赖完整性 | 本地 NuGet 缓存 100/100 个包的 SHA-512 均与缓存校验文件一致 |
| NuGet 官方安全公告 | Core、App、Tests 的直接与传递依赖均为 0 个已知漏洞；`NU1901`～`NU1904` 已设为构建错误 |
| PowerShell 语法 | 所有 `scripts/*.ps1` 通过 AST 解析 |
| Git 补丁检查 | `git diff --check` 无空白错误；仅有行尾转换提示 |
| 正式发布 | `scripts/build.ps1` 完成恢复、测试和自包含单文件发布 |

重点覆盖：

- 高水位 Token 增量、轻微计数回退、文件截断、等长改写、追加和半行补全；
- 子代理与显式 fork 的复制历史去重、迁移重放和父任务尚未入库；
- 首次索引阶段、字节进度单调性、锁文件失败、重试、取消和重建；
- 首次索引未完成时不写完成标记，后续启动或明确操作可继续；
- 单实例的提前激活、重复激活、释放后重新取得主实例；
- 关机中的 WPF Dispatcher 投递被安全忽略，不反向中断索引或清理；
- 本地诊断日志追加、1 MiB 轮转和日志失败不影响应用；
- UI/XAML、双语资源、主题、待机样式、息屏暂停与审计脚本安全契约。

## 依赖安全审计

经用户授权后，使用 NuGet 官方源对解决方案的直接与传递依赖执行安全公告查询。
首轮发现 `SQLitePCLRaw.lib.e_sqlite3 2.1.6` 命中一个高危公告
[`GHSA-2m69-gcr7-jv3q`](https://github.com/advisories/GHSA-2m69-gcr7-jv3q)；
测试工程另有两个只存在于旧测试工具依赖链中的高危包。

本轮采用最小兼容修复：保留 `Microsoft.Data.Sqlite 8.0.21`，将原生 SQLite bundle
显式固定到 `SQLitePCLRaw.bundle_e_sqlite3 2.1.12`，并升级测试 SDK、xUnit 与覆盖率
收集器。重新从 NuGet 官方源恢复后，Core、App、Tests 均报告没有已知漏洞；随后
严格构建、345 项测试、自包含发布、单实例黑盒及 65 秒静默复测均通过。仓库同时
启用了直接与传递依赖审计，今后 `NU1901`～`NU1904` 会阻止构建或发布；仅公告源
暂时无法连接的 `NU1900` 保持为可见警告，避免离线开发被误锁死。

## 限额窗口兼容性

OpenAI [当前官方定价文档](https://learn.chatgpt.com/docs/pricing)仍描述五小时窗口，
并说明可能同时存在周限制；官方 [Codex App Server 协议](https://learn.chatgpt.com/docs/app-server)
则公开了 `account/rateLimits/read`、变更通知、多 bucket 结果，以及
`usedPercent`、`windowDurationMins`、`resetsAt` 等字段。

本机只读证据显示：正式索引中曾记录 3,215 个 300 分钟窗口，时间范围为
2026-06-17 22:18 至 2026-07-11 22:04（Asia/Shanghai）；10080 分钟窗口仍持续
出现。最近日志只返回周窗口，短窗口为空。这说明当前账户的客户端行为与官方通用
文档并不完全同步，但并不代表接口或五小时窗口已经永久移除。

现有模型、日志解析器和窗口命名逻辑已能同时处理 primary/secondary、300 分钟与
10080 分钟窗口，收起态也会采用剩余量更紧张的窗口。当前未绑定的是“同时明确列出
两个窗口”的独立 UI，也未引入额外 App Server 进程。建议保持低资源日志路径，先
做“300 分钟窗口存在时才显示、缺失时完全折叠”的条件 UI；只有未来日志不再提供
恢复后的短窗口，才评估接入 App Server。这样现在不产生额外轮询或常驻开销。

## 真实日志与数据库

对当前正式索引执行只读检查：

```text
PRAGMA quick_check = ok
foreign_key_violations = 0
outdated_accounting_rows = 0
initial_index_complete = 1
file_state = 847
token_events = 69,684
daily_task_usage = 206
total_tokens = 9,049,602,701
```

另以 `D:\Codex\Home` 为只读来源，在项目忽略目录创建全新隔离数据库并完整重建：

```text
日志文件：854
枚举总字节：18,114,323,308
首轮实际处理字节：18,114,366,919（读取期间活动日志继续追加）
file_state：854
token_events：71,226
daily_task_usage：211
PRAGMA quick_check：ok
foreign_key_violations：0
会计版本过期行：0
首次索引完成标记：1
首轮总量：9,288,668,988
增量复核：2 个变化文件、73,788 字节，总量 9,290,110,767
```

重建后的增量复核记录了完整阶段：

```text
Preparing → Reading → Finalizing → Completed
```

首轮读取进度从 0 到 1，所有样本均位于 `[0,1]` 且单调不回退；第二次刷新完成，
无新增时为 0 个变化文件、0 个处理字节，快照总量保持一致。审计期间当前 Codex
任务仍可能追加日志，因此真实来源测试把“第二次必须零变化”只用于静止时段，
不会把合法追加误报为索引错误。

## 运行时黑盒

### 单实例

最终 EXE 连续启动两次：第二进程退出码为 0，运行实例数保持 1，主进程继续存活；
测试结束后审计进程全部清理，设置按原始字节恢复。机器报告位于被 Git 忽略的
`artifacts/runtime/single-instance-smoke.json`。

### 收起静默

圆环待机完成 12 秒预热、5 秒索引稳定确认和 65.013 秒正式观察：

| 指标 | 实测 |
|---|---:|
| 单核口径 CPU | 0.024% |
| 16 线程整机口径 CPU | 0.0015% |
| 私有内存 | 78.05 MiB |
| 峰值私有内存 | 78.17 MiB |
| 工作集 | 165.75 MiB |
| SQLite 正式观察期写入 | 0 |
| 起止窗口尺寸 | 80×80 / 80×80 |
| 结果 | `validationPassed=true` |

原始数据见 [`idle-performance.json`](idle-performance.json)。该测试不依赖鼠标，
因此锁屏不会使结果失效。

### UI 综合验收

解锁桌面上的综合 UI 验收共 16 项，全部通过：80×80 收起、420×540 展开、四个
周期标签、周额度浮层、固定、拖动、62 毫秒即时收起、屏幕边缘展开与锚点恢复、
设置页以及最终收起。运行环境为 200% DPI，截图和报告均由同一份 v1.2.0 EXE 生成：

- [`screenshots/ui-audit.json`](screenshots/ui-audit.json)

待机样式专项验收另行覆盖：

- 微光：32×32，直接展开到 420×540，再收回 32×32；
- 圆环：80×80，直接展开到 420×540，再收回 80×80；
- 胶囊：208×80，直接展开到 420×540，再收回 208×80。

三种样式均未出现中间顶层窗口尺寸，结果为 3/3 通过；证据见
[`idle-style-audit.json`](idle-style-audit.json)。中英文切换、设置项和主界面关键文本
也已通过 UI Automation 验收，证据见 [`localization-audit.json`](localization-audit.json)。

## 本地异常诊断

未知后台异常、未观察任务异常、启动异常和 Dispatcher 异常会以尽力而为方式写入：

```text
%LocalAppData%\CodexUsageWidget\diagnostics.log
```

日志只保存在本机，达到 1 MiB 后轮转为 `diagnostics.log.previous`。记录失败不会再次
导致应用失败。本轮运行验收前后日志长度与修改时间均未变化，未产生新的异常记录。

## 隐私与联网检查

源码未发现 `HttpClient`、WebRequest、Socket、遥测、上传或下载路径，运行时不主动
联网。应用不会打开 `auth.json`、密码或访问令牌。解析器会在本机顺序读取 JSONL
行以识别事件类型，但不提取、持久化或上传用户提示词和聊天正文；SQLite 只保存
统计字段、任务标题和增量检查点。

## 尚未闭合的外部或硬件边界

- NuGet 安全状态只代表 2026-08-03 查询时官方公告库中已知的问题；新公告发布后
  仍需重新恢复或执行依赖审计。
- 当前只有单显示器 200% DPI 环境；负坐标多显示器、跨不同 DPI 显示器和向上展开
  仍属于代码与自动化契约覆盖，缺少本机硬件黑盒证据。
- Codex 本地日志属于内部格式；未来客户端改变字段时仍可能需要更新解析器。
- 累计值只覆盖本机仍可读取或曾被索引的日志，不等同于服务端账户统计。

因此当前可以确认“已执行项目全部通过”，但不能诚实声称数学意义上的 100% 无漏洞。
NuGet 官方公告查询与当前桌面 UI 验收已经闭合；尚无对应硬件的多显示器场景仍属于
外部边界，所以本轮验收状态是“有边界地通过”。
